node_modules/
dist/
.turbo/
*.tsbuildinfo
.DS_Store
.env
.env.local
# downloaded avatars/props (regenerate with `pnpm assets`)
apps/client/public/models/
packages/assets/.cache/

# SSH host keys — generated per deployment, never committed
# (each game server prints the ssh-keygen command on first run)
apps/*/assets/host.key
apps/*/assets/host.key.pub
